---
import type { Tag } from '../consts';
import { TAGS } from '../consts';

interface Props {
	tags: Tag[];
}

const { tags } = Astro.props;
---

<div class="inline-flex gap-2">
	{tags.map((tag) => (
		<a href={`/tags/${tag}/`} class="badge badge-outline">{TAGS[tag]}</a>
	))}
</div>
